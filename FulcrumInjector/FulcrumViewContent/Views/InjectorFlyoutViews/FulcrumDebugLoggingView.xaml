<UserControl x:Class="FulcrumInjector.FulcrumViewContent.Views.InjectorFlyoutViews.FulcrumDebugLoggingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:avEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:viewModels="clr-namespace:FulcrumInjector.FulcrumViewContent.ViewModels"
             xmlns:injectorFlyoutViewModels="clr-namespace:FulcrumInjector.FulcrumViewContent.ViewModels.InjectorFlyoutViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             Loaded="FulcrumInjectorDebugLoggingView_OnLoaded">

    <!-- DataContext Binding -->
    <UserControl.DataContext>
        <injectorFlyoutViewModels:FulcrumDebugLoggingViewModel/>
    </UserControl.DataContext>

    <!-- Style Sheets -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../FulcrumViewSupport/AppStyleSheets/FulcrumControlStyles.xaml"/>
                <ResourceDictionary Source="../../../FulcrumViewSupport/AppStyleSheets/FulcrumDebugLoggingViewStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Log Viewing Content -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".15*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!-- Log Filter and Search Controls -->
        <Grid Grid.Row="0" Margin="10,15,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width=".5*"/>
            </Grid.ColumnDefinitions>
            <TextBox 
                Style="{StaticResource SearchBoxStyle}"
                Tag="Enter text to find or type '@r' plus a regex"
                VerticalAlignment="Stretch"
                TextChanged="LogFilteringTextBox_OnTextChanged"/>
            <ComboBox
                IsEnabled="True"
                Grid.Column="1" 
                FontSize="13" 
                SelectedIndex="0"
                HorizontalContentAlignment="Center" 
                VerticalContentAlignment="Center"
                Margin="10,0"
                DropDownOpened="LoggerNameComboBox_OnDropDownOpened"
                SelectionChanged="LoggerNameComboBox_OnSelectionChanged"
                Style="{StaticResource MahApps.Styles.ComboBox}"
                ItemsSource="{Binding Path=LoggerNamesFound}"/>
        </Grid>

        <!-- Logging Content Output -->
        <avEdit:TextEditor 
            Grid.Row="1" 
            x:Name="DebugRedirectOutputEdit"
            Style="{StaticResource LoggingOutputBoxStyle}"/>
    </Grid>
</UserControl>
